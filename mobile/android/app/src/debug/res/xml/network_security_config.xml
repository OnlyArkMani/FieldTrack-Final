<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-ONLY override (src/debug resources shadow src/main of the same name, so
  this file is used ONLY in debug builds; the release APK uses the strict
  src/main version). Permits cleartext to localhost + private LAN ranges so
  `flutter run` can hit an http:// dev backend (e.g. 10.0.2.2 emulator host,
  192.168.x.x). Public traffic still requires HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
</network-security-config>
